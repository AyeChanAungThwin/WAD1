<Window x:Class="WpfLocalizationTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfLocalizationTest"
        mc:Ignorable="d"
        Title="{StaticResource Title}" Height="350" Width="525">
    <Grid>
        <Menu>
            <MenuItem 
                Header="{StaticResource English}"
                Click="MenuItem_OnClickEnglish"/>
            <MenuItem 
                Header="{StaticResource Myanmar}"
                Click="MenuItem_OnClickMyanmar"/>
        </Menu>
        
        <TextBlock
            Text="{StaticResource Greeting}"
            FontSize="40"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"/>
    </Grid>
</Window>
